{
  "name": "boluo-2",
  "version": "1.0.0",
  "description": "菠萝2号 - 专属AI助手桌面客户端",
  "author": "菠萝2号",
  "license": "MIT",
  "private": true,
  "scripts": {
    "serve": "npx serve . -p 3000",
    "build:apk": "npx cap sync && cd android && ./gradlew assembleDebug",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "release": "npx cap sync && cd android && ./gradlew bundleRelease"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "serve": "^14.0.0"
  }
}